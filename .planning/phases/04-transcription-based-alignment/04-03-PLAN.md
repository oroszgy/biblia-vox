---
phase: 04-transcription-based-alignment
plan: 3
type: execute
wave: 3
depends_on: [2]
files_modified:
  - "bibliavox/align/match.py"
  - "bibliavox/cli/align.py"
autonomous: true
requirements:
  - ALN-02
must_haves:
  truths:
    - "User can run align:match command"
    - "RapidFuzz word-level matching generates per-verse timestamps"
  artifacts:
    - path: "bibliavox/align/match.py"
      provides: "Word-level sliding window fuzzy matching"
  key_links:
    - from: "bibliavox/align/match.py"
      to: "bibliavox/align/transcribe.py"
      via: "JSON intermediate format"
---

<objective>
Implement RapidFuzz sliding-window matching to map canonical verse texts to the word-level transcriptions.

Purpose: Translate raw transcribed speech into exact Bible verse timestamps with confidence scores.
Output: Matching module and `align match` CLI command.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-transcription-based-alignment/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>task 1: implement fuzzy matching</name>
  <files>bibliavox/align/match.py</files>
  <read_first>
    - bibliavox/align/match.py
  </read_first>
  <action>
    Create `bibliavox/align/match.py`. Implement `match_verses(verse_texts, word_transcripts)`.
    Per D-02, use RapidFuzz (`rapidfuzz.process.extract` or sliding window over word arrays) to match the sequence of words in a verse against the transcribed words.
    Return a list of dictionaries with `verse_id`, `start_sec`, `end_sec`, and `confidence_score`.
  </action>
  <acceptance_criteria>
    - `bibliavox/align/match.py` imports `rapidfuzz`
    - Returns timestamps based on word matches
  </acceptance_criteria>
  <done>RapidFuzz sliding-window matcher is implemented.</done>
</task>

<task type="auto">
  <name>task 2: implement match CLI subcommand</name>
  <files>bibliavox/cli/align.py</files>
  <read_first>
    - bibliavox/cli/align.py
  </read_first>
  <action>
    Update `bibliavox/cli/align.py` to add a `match` command.
    It accepts `--book`, `--chapter`, and `--model`. It reads the intermediate transcript JSON, reads the canonical text for that chapter from the Phase 2 JSONL (`data/processed/text/szit.jsonl`), runs `match_verses`, and outputs the final verse timestamps to `data/processed/align/{USX}_{CHAPTER}_{model}_verses.json`.
  </action>
  <acceptance_criteria>
    - `bibliavox/cli/align.py` contains `@app.command("match")`
    - Outputs verse timestamps to `data/processed/align/`
  </acceptance_criteria>
  <done>CLI can trigger alignment matching.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File I/O | Reading intermediate transcript |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03   | Information Disclosure | Verse matches | accept | Output contains public domain/Bible texts and local timestamps, no sensitive data. |
</threat_model>
