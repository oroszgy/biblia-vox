---
phase: 04-transcription-based-alignment
plan: 2
type: execute
wave: 2
depends_on: [1]
files_modified:
  - "bibliavox/align/transcribe.py"
  - "bibliavox/cli/align.py"
  - "bibliavox/main.py"
autonomous: true
requirements:
  - ALN-01
  - ALN-08
must_haves:
  truths:
    - "User can run align:transcribe command"
    - "Transcription uses silero VAD to skip silences"
    - "Output is intermediate word-level JSON"
  artifacts:
    - path: "bibliavox/align/transcribe.py"
      provides: "faster-whisper and vibevoice logic"
    - path: "bibliavox/cli/align.py"
      provides: "Typer subcommands for alignment"
  key_links:
    - from: "bibliavox/cli/align.py"
      to: "bibliavox/align/transcribe.py"
      via: "command invocation"
---

<objective>
Implement the core transcription engine supporting faster-whisper with Hungarian LoRA and VibeVoice, producing word-level intermediate transcripts.

Purpose: Generate accurate phonetic mapping of the audio with precise word boundaries, ignoring silences to avoid phantom verses.
Output: Transcription module and Typer CLI command.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-transcription-based-alignment/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>task 1: implement transcription backend</name>
  <files>bibliavox/align/transcribe.py</files>
  <read_first>
    - bibliavox/align/transcribe.py
    - bibliavox/config.py
  </read_first>
  <action>
    Create `bibliavox/align/transcribe.py`. Implement a function `transcribe_audio(audio_path, model_config)`.
    Per D-01, for faster-whisper, instantiate the model with `vad_filter=True` (using silero VAD) to skip silences.
    Enable `word_timestamps=True` in the transcription call.
    Also implement a branching path to support VibeVoice if `model_config.type == 'vibevoice'` (per D-05).
    Return a list of dictionaries with word, start, and end keys.
  </action>
  <acceptance_criteria>
    - `bibliavox/align/transcribe.py` contains `vad_filter=True`
    - `bibliavox/align/transcribe.py` contains `word_timestamps=True`
  </acceptance_criteria>
  <done>Transcription backends are implemented.</done>
</task>

<task type="auto">
  <name>task 2: implement align CLI subcommand</name>
  <files>bibliavox/cli/align.py, bibliavox/main.py</files>
  <read_first>
    - bibliavox/cli/align.py
    - bibliavox/main.py
  </read_first>
  <action>
    Create `bibliavox/cli/align.py` with a Typer app. Implement a `transcribe` command that accepts `--book`, `--chapter`, and `--model` (or defaults to gauntlet).
    It should load the WAV from `data/prepared/audio/{USX}/`, pass it to `transcribe_audio`, and write the word-level output to `data/processed/align/{USX}_{CHAPTER}_{model}_transcript.json`.
    Update `bibliavox/main.py` to include the `align` subcommand.
  </action>
  <acceptance_criteria>
    - `bibliavox/cli/align.py` contains `@app.command("transcribe")`
    - `bibliavox/main.py` registers the `align` Typer group
  </acceptance_criteria>
  <done>CLI can trigger transcription.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Process -> File System | Writing transcripts to disk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02   | Denial of Service | Transcription Engine | accept | Out-of-memory errors on GPU handled via container restart, acceptable for CLI tool. |
</threat_model>
