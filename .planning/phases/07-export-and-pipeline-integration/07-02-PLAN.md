---
phase: 07-export-and-pipeline-integration
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - Taskfile.yml
autonomous: true
requirements:
  - EXP-03
  - EXP-05

must_haves:
  truths:
    - "User can run task export:jsonl GOLD=true and get JSONL output for gold chapters"
    - "User can run task export:run GOLD=true and see the full pipeline execute end-to-end"
    - "Re-running task export:run skips already-completed chapters unless FORCE=true"
    - "Pipeline fails fast on first stage failure"
    - "Default model is VibeVoice (microsoft/VibeVoice-ASR-HF)"
  artifacts:
    - path: "Taskfile.yml"
      provides: "Export and pipeline Taskfile targets"
      contains: "export:jsonl"
  key_links:
    - from: "Taskfile.yml export:jsonl"
      to: "bibliavox/cli/export.py"
      via: "uv run bibliavox export jsonl --gold"
      pattern: "bibliavox export jsonl"
    - from: "Taskfile.yml export:run"
      to: "export:jsonl"
      via: "deps chain"
      pattern: "deps:.*export:jsonl"
---

<objective>
Add Taskfile targets for export and end-to-end pipeline orchestration.

Purpose: Wire the export CLI into reproducible Taskfile targets that chain all pipeline stages (text → audio → alignment → export) into a single `task export:run` command. This completes the "last mile" integration making the pipeline runnable by non-technical operators.

Output:
- `Taskfile.yml` — New `export:jsonl`, `export:fetch-text`, `export:prepare-audio`, `export:align`, and `export:run` targets
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-export-and-pipeline-integration/07-CONTEXT.md
@.planning/phases/07-export-and-pipeline-integration/07-RESEARCH.md

@Taskfile.yml
@bibliavox/cli/export.py
@bibliavox/config.py

<interfaces>
<!-- Key types and contracts the executor needs. -->

From Taskfile.yml (existing task pattern):
```yaml
  audio:prepare:
    desc: Prepare chapter artifacts (wav + meta + index) from raw MP3
    deps: [audio:download]
    cmds:
      - uv run bibliavox audio prepare --book {{.BOOK}} --chapter {{.CHAPTER}}{{if .FORCE}} --force{{end}}
```

From Taskfile.yml (variable syntax — go-task does NOT support CLI flags):
```yaml
  # NOTE: go-task parses task-level flags itself, so command flags like
  # `task audio:download --book GEN --chapter 1` are not supported.
  # Use task vars (BOOK/CHAPTER/WORKERS) intentionally for this contract.
```

From bibliavox/cli/export.py (created in Plan 01):
```python
@app.command("jsonl")
def jsonl_command(
    gold: bool = typer.Option(False, help="Export gold chapters only"),
    model: str = typer.Option(None, help="Specific model ID to export"),
    force: bool = typer.Option(False, help="Force re-export even if complete"),
) -> None:
```

From bibliavox/config.py (Plan 01 addition):
```python
gold_chapters: str = "TIT 1,TIT 2,TIT 3,TOB 1,TOB 2,TOB 3,TOB 4,ZEP 1,ZEP 2,ZEP 3"
```

From .planning/phases/07-export-and-pipeline-integration/07-CONTEXT.md:
- D-08: Chained Taskfile targets: export:fetch-text, export:prepare-audio, export:align, export:jsonl
- D-09: Fail-fast on model failure
- D-10: Default model: VibeVoice only (microsoft/VibeVoice-ASR-HF)
- D-14: --force flag triggers full re-run
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add export and pipeline targets to Taskfile.yml</name>
  <files>Taskfile.yml</files>
  <read_first>
    - Taskfile.yml (full file — understand existing structure, variable syntax, deps pattern)
    - bibliavox/cli/export.py (CLI command signatures — gold, model, force flags)
    - .planning/phases/07-export-and-pipeline-integration/07-CONTEXT.md (D-08 through D-14)
  </read_first>
  <action>
Add the following targets to `Taskfile.yml` after the existing `align:*` targets:

```yaml
  export:fetch-text:
    desc: Ensure canonical text corpus is available (mek.jsonl)
    cmds:
      - task: text:ingest-mek
    status:
      - test -f data/processed/text/mek.jsonl

  export:prepare-audio:
    desc: Prepare audio for gold chapters (download + convert + index)
    cmds:
      - task: audio:prepare-all

  export:align:
    desc: Run alignment on gold chapters in Docker with GPU
    cmds:
      - docker compose run --rm align python -m bibliavox.main align evaluate-gold {{if .MODEL}}--model "{{.MODEL}}"{{end}}
    status:
      # Skip if evaluation data exists with all gold chapters covered and not forced
      - '{{if .FORCE}}false{{else}}uv run python -c "import json, sys; s=json.load(open(\"data/evaluation/summary.json\")); chapters={e.get(\"chapter\",\"\") for e in (s if isinstance(s,list) else s.get(\"chapters\",s.get(\"results\",[])))}; gold=[\"TIT 1\",\"TIT 2\",\"TIT 3\",\"TOB 1\",\"TOB 2\",\"TOB 3\",\"TOB 4\",\"ZEP 1\",\"ZEP 2\",\"ZEP 3\"]; sys.exit(0 if all(g in chapters for g in gold) else 1)"{{end}}'

  export:jsonl:
    desc: Export alignment results to JSONL (GOLD=true for gold chapters, MODEL=... to override)
    vars:
      MODEL: '{{default "microsoft/VibeVoice-ASR-HF" .MODEL}}'
    cmds:
      - uv run bibliavox export jsonl --gold --model "{{.MODEL}}"{{if .FORCE}} --force{{end}}
    deps:
      - export:fetch-text

  export:run:
    desc: Run full pipeline on gold chapters (text → audio → align → export)
    vars:
      MODEL: '{{default "microsoft/VibeVoice-ASR-HF" .MODEL}}'
    cmds:
      - task: export:jsonl
        vars:
          GOLD: "true"
          MODEL: "{{.MODEL}}"
          FORCE: "{{.FORCE}}"
    deps:
      - export:prepare-audio
      - export:align
```

Key design decisions:
- D-08: `export:run` chains `export:prepare-audio` → `export:align` → `export:jsonl` via deps
- D-09: Fail-fast — if any dep fails, go-task stops (built-in behavior)
- D-10: Default MODEL is `microsoft/VibeVoice-ASR-HF` via `{{default ...}}`
- D-14: FORCE variable passed through to disable idempotency checks
- Variable syntax uses go-task `{{.VAR}}` pattern (not CLI flags) per existing convention
- `export:align` uses `status` to skip if evaluation data exists (unless FORCE=true)
- `export:fetch-text` uses `status` to skip if mek.jsonl already exists
  </action>
  <verify>
    <automated>task --list 2>&1 | grep -q "export:jsonl" && task --list 2>&1 | grep -q "export:run"</automated>
  </verify>
  <acceptance_criteria>
    - Taskfile.yml contains `export:fetch-text:` target
    - Taskfile.yml contains `export:prepare-audio:` target
    - Taskfile.yml contains `export:align:` target
    - Taskfile.yml contains `export:jsonl:` target with `uv run bibliavox export jsonl --gold`
    - Taskfile.yml contains `export:run:` target with deps on export:prepare-audio and export:align
    - `task --list` shows export:fetch-text, export:prepare-audio, export:align, export:jsonl, export:run
    - export:jsonl uses MODEL variable with default "microsoft/VibeVoice-ASR-HF"
    - export:run uses FORCE variable for --force passthrough
    - export:align has status check that verifies summary.json contains entries for all gold chapters (not just file existence)
  </acceptance_criteria>
  <done>
    - All 5 export Taskfile targets added and working
    - `task export:run GOLD=true MODEL=vibevoice FORCE=true` chains all stages
    - Default model is VibeVoice (D-10)
    - Idempotency via status checks (D-13)
    - Force passthrough works (D-14)
  </done>
</task>

<task type="auto">
  <name>Task 2: Add integration test for pipeline chain wiring</name>
  <files>tests/test_pipeline_integration.py</files>
  <read_first>
    - Taskfile.yml (verify export targets are correctly chained)
    - bibliavox/cli/export.py (CLI command signatures)
    - bibliavox/export/writer.py (export writer functions)
    - bibliavox/config.py (gold_chapters config)
  </read_first>
  <action>
Create `tests/test_pipeline_integration.py` with integration tests that verify the pipeline chain wiring (not just CLI help or lint):

1. `test_export_cli_jsonl_subcommand_exists`: Invoke `bibliavox export --help` via CliRunner and assert "jsonl" appears in output.

2. `test_export_jsonl_help_shows_all_options`: Invoke `bibliavox export jsonl --help` via CliRunner and assert --gold, --model, --force options are present.

3. `test_export_jsonl_gold_with_mock_data`: Create a minimal matched JSON file and mek.jsonl in tmp_path. Invoke `bibliavox export jsonl --gold` with BIBLIAVOX_DATA_DIR pointing to tmp_path. Verify JSONL output file is created with correct fields (verse_ref, audio_file, start_sec, end_sec, source, translation, confidence, canonical_text, matched_text, wer, cer).

4. `test_export_jsonl_force_overwrites_complete`: Create a complete JSONL export file, then invoke with --force. Verify the file is re-written (not skipped).

5. `test_taskfile_export_targets_present`: Run `task --list` and verify all 5 export targets (export:fetch-text, export:prepare-audio, export:align, export:jsonl, export:run) are listed.

6. `test_parse_gold_chapters_integration`: Verify parse_gold_chapters("TIT 1,TIT 2,TIT 3") returns [("TIT", 1), ("TIT", 2), ("TIT", 3)] — confirms config wiring works end-to-end.

These tests verify that the CLI, config, writer module, and Taskfile are wired together correctly — not just that individual pieces exist.
  </action>
  <verify>
    <automated>uv run pytest tests/test_pipeline_integration.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_pipeline_integration.py exists
    - test_export_jsonl_gold_with_mock_data passes (verifies full CLI→writer→output chain)
    - test_taskfile_export_targets_present passes (verifies Taskfile targets are registered)
    - All 6 integration tests pass
  </acceptance_criteria>
  <done>
    - Integration tests verify pipeline chain wiring end-to-end
    - CLI→config→writer→output chain tested with mock data
    - Taskfile target presence verified
    - Phase 7 complete: export module, CLI, config, Taskfile targets all integrated and tested
  </done>
</task>

</tasks>

<verification>
```bash
# Taskfile targets exist
task --list | grep export

# CLI works
uv run bibliavox export --help
uv run bibliavox export jsonl --help

# Tests pass
uv run pytest tests/test_export_writer.py tests/test_config.py -x -v

# Quality
uv run ruff check bibliavox/export/ bibliavox/cli/export.py
uv run ruff format --check bibliavox/export/ bibliavox/cli/export.py
```
</verification>

<success_criteria>
- `task export:jsonl GOLD=true` produces JSONL for gold chapters (EXP-03)
- `task export:run GOLD=true` runs full pipeline end-to-end (EXP-05)
- Re-running skips completed chapters unless FORCE=true (D-13, D-14)
- Default model is VibeVoice (D-10)
- Fail-fast on stage failure (D-09)
- All 5 export targets in Taskfile (EXP-03)
</success_criteria>

<output>
After completion, create `.planning/phases/07-export-and-pipeline-integration/07-02-SUMMARY.md`
</output>
