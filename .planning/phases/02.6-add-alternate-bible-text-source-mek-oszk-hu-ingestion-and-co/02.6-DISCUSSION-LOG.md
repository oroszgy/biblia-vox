# Phase 2.6: Discussion Log

**Date:** 2026-05-30
**Phase:** 2.6 — Alternate Text Source & Cross-Source Coverage Validation

## Areas Discussed

### 1. MEK HTML Parsing Approach

**Options presented:** BeautifulSoup / lxml with XPath / Regex on raw HTML / Inspect first then decide

**User selection:** BeautifulSoup (Recommended)

**Notes:** Explicit DOM navigation, robust to whitespace/attribute variation, easy to maintain.

---

### 2. Coverage Scope

**Options presented:** Full 73-book pass / Gold subset only / Best-effort: all available, flag missing

**User selection:** Full 73-book pass (Recommended)

**Notes:** If MEK does not have a book, report as missing rather than error.

---

### 3. Cross-Source Comparison Output

**Options presented:** New CLI command + JSONL diff / Extend existing validate command / JSONL diff file only

**User selection:** New CLI command + JSONL diff (Recommended)

**Notes:** `bibliavox text cross-validate` prints Rich table to stdout and writes JSONL diff to `data/processed/text/`. Severity levels: `missing_verse`, `missing_chapter`, `missing_book`, `text_diff`.

---

### 4. MEK Text Storage

**Options presented:** `data/processed/text/mek.jsonl` only / Raw HTML + processed JSONL / Subdirectory for multi-source

**User selection:** Both tiers — raw HTML in `data/raw/text/mek/`, processed JSONL in `data/processed/text/mek.jsonl`

**Notes:** Chapter-per-file raw HTML cache (`{BOOK}_{CHAPTER}.html`), processed to same JSONL schema as szit-fixed.jsonl.

---

## Deferred Ideas

None.

## OpenCode Discretion

None — all gray areas resolved by user.
