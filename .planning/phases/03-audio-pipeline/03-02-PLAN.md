---
phase: 03-audio-pipeline
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - bibliavox/audio/convert.py
  - bibliavox/audio/metadata.py
  - bibliavox/audio/__init__.py
  - bibliavox/cli/audio.py
  - Taskfile.yml
  - tests/test_audio_convert.py
  - tests/test_audio_metadata.py
autonomous: true
requirements: [AUD-03, AUD-04]
must_haves:
  truths:
    - "D-05/D-06: User can convert downloaded chapter MP3 into WAV 16kHz mono PCM only when ffmpeg/ffprobe dependencies are present, with explicit setup-guided failure otherwise"
    - "D-05/D-06: Converted WAV format is verified, not assumed"
    - "D-04/D-09: User can inspect audio metadata via CLI info command bound to canonical artifact layout"
  artifacts:
    - path: "bibliavox/audio/convert.py"
      provides: "ffmpeg conversion orchestration and post-conversion invariant checks"
      exports: ["convert_to_wav"]
    - path: "bibliavox/audio/metadata.py"
      provides: "ffprobe metadata extraction and normalization"
      exports: ["probe_audio", "format_audio_info"]
    - path: "bibliavox/cli/audio.py"
      provides: "audio convert and info CLI commands"
      exports: ["convert", "info"]
  key_links:
    - from: "bibliavox/audio/convert.py"
      to: "bibliavox/audio/metadata.py"
      via: "post-conversion probe assertion"
      pattern: "probe_audio"
    - from: "bibliavox/cli/audio.py"
      to: "bibliavox/audio/convert.py"
      via: "CLI convert command invokes convert_to_wav"
      pattern: "convert_to_wav"
---

<objective>
Implement deterministic MP3→WAV conversion and metadata inspection commands.

Purpose: satisfy AUD-03 and AUD-04 by enforcing VBR-safe WAV preprocessing (16kHz mono PCM), strict ffmpeg/ffprobe dependency policy, and deterministic metadata reporting for downstream alignment diagnostics.

Output: conversion and metadata modules, `bibliavox audio convert`, `bibliavox audio info`, plus Taskfile conversion tasks.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-audio-pipeline/03-RESEARCH.md
@.planning/phases/03-audio-pipeline/03-01-PLAN.md
@bibliavox/cli/audio.py

<interfaces>
From bibliavox/audio/downloader.py (created in 03-01):
```python
def download_chapter(...) -> Path
def download_all(...) -> dict[str, int]
```

From expected storage convention in research:
```text
data/raw/audio/{USX}/{chapter:03d}.mp3
data/prepared/audio/{USX}/{chapter:03d}.wav
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: implement ffmpeg conversion with strict WAV invariants</name>
  <files>bibliavox/audio/convert.py, tests/test_audio_convert.py</files>
  <read_first>
    - .planning/phases/03-audio-pipeline/03-RESEARCH.md (ffmpeg command contract and invariants)
    - bibliavox/audio/downloader.py (source file path conventions)
    - tests/test_audio_downloader.py (mocking subprocess/network style patterns)
  </read_first>
  <behavior>
    - Test 1: convert_to_wav invokes ffmpeg with `-ac 1 -ar 16000 -c:a pcm_s16le`
    - Test 2: conversion fails with explicit error when ffmpeg exits non-zero
    - Test 3: conversion rejects output when probe says sample_rate/channels/codec do not match required values
  </behavior>
  <action>Create `bibliavox/audio/convert.py` implementing `convert_to_wav(input_mp3, output_wav)` for AUD-03. Before conversion, assert `ffmpeg` binary exists; if missing, fail with explicit setup guidance (per D-05, D-06). Run ffmpeg via argument-list subprocess (no shell) with deterministic flags (`-y -ac 1 -ar 16000 -c:a pcm_s16le`). After process success, call metadata probe and enforce invariants: codec `pcm_s16le`, sample rate `16000`, channels `1`; if any mismatch, raise a typed conversion error and do not mark chapter as prepared. Add tests first in `tests/test_audio_convert.py` with subprocess mocking and invariant assertion coverage (RED→GREEN).</action>
  <verify>
    <automated>uv run pytest tests/test_audio_convert.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "def convert_to_wav\(" bibliavox/audio/convert.py` returns a match
    - `rg -n "pcm_s16le" bibliavox/audio/convert.py` returns a match
    - `rg -n "16000" bibliavox/audio/convert.py` returns a match
    - `uv run pytest tests/test_audio_convert.py -x -v` exits 0
  </acceptance_criteria>
  <done>Conversion produces validated 16k mono PCM WAV output suitable for alignment stage input.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: implement ffprobe metadata extractor and CLI info rendering</name>
  <files>bibliavox/audio/metadata.py, bibliavox/cli/audio.py, tests/test_audio_metadata.py</files>
  <read_first>
    - .planning/phases/03-audio-pipeline/03-RESEARCH.md (ffprobe JSON format and required fields)
    - bibliavox/cli/text.py (Rich console output conventions)
    - bibliavox/audio/convert.py (how probe results are consumed)
  </read_first>
  <behavior>
    - Test 1: probe_audio returns normalized dict containing duration/bitrate/sample_rate/channels/codec
    - Test 2: audio info command renders metadata for a chapter path
    - Test 3: ffprobe failure surfaces a clear CLI error and non-zero exit
  </behavior>
  <action>Create `bibliavox/audio/metadata.py` with `probe_audio(path)` that runs ffprobe JSON and normalizes required fields for AUD-04 (`duration`, `bit_rate`, `sample_rate`, `channels`, `codec_name`). Assert `ffprobe` availability up front and fail with setup guidance when missing (per D-05, D-06). Add helper `format_audio_info` for CLI display. Extend `bibliavox/cli/audio.py` with `audio info --book --chapter` and `audio convert --book --chapter` commands using `data/raw/audio/{USX}/{chapter:03d}.mp3` and `data/prepared/audio/{USX}/{chapter:03d}.wav` path contract (per D-09). Keep output deterministic and include full file path for traceability.</action>
  <verify>
    <automated>uv run pytest tests/test_audio_metadata.py -x -v && uv run bibliavox audio info --help</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "def probe_audio\(" bibliavox/audio/metadata.py` returns a match
    - `rg -n "ffprobe" bibliavox/audio/metadata.py` returns a match
    - `rg -n "def info\(" bibliavox/cli/audio.py` returns a match
    - `rg -n "def convert\(" bibliavox/cli/audio.py` returns a match
    - `uv run pytest tests/test_audio_metadata.py -x -v` exits 0
  </acceptance_criteria>
  <done>Metadata extraction and user-facing audio info command are available and verified by tests.</done>
</task>

<task type="auto">
  <name>task 3: wire conversion Taskfile targets and run smoke checks</name>
  <files>Taskfile.yml, bibliavox/audio/__init__.py</files>
  <read_first>
    - Taskfile.yml (existing naming/dependency style)
    - bibliavox/audio/convert.py and bibliavox/audio/metadata.py (new exports)
    - .planning/ROADMAP.md (Phase 3 success criteria commands)
  </read_first>
  <action>Update `bibliavox/audio/__init__.py` to export conversion and metadata functions. Add Taskfile targets: `audio:convert` for single chapter, `audio:convert-all` for batch conversion, and `audio:info` for metadata inspection (per D-04). Ensure targets depend on prior download artifacts and preserve idempotent skip-by-default semantics with optional force override (per D-12). Include explicit task notes that conversion/info require ffmpeg+ffprobe installed and that missing binaries are hard failures with guidance (per D-05, D-06).</action>
  <verify>
    <automated>uv run bibliavox audio convert --help && uv run bibliavox audio info --help</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "audio:convert:" Taskfile.yml` returns a match
    - `rg -n "audio:info:" Taskfile.yml` returns a match
    - `rg -n "convert_to_wav" bibliavox/audio/__init__.py` returns a match
    - `rg -n "probe_audio" bibliavox/audio/__init__.py` returns a match
    - `uv run bibliavox audio convert --help` exits 0
  </acceptance_criteria>
  <done>Taskfile and package exports expose conversion/info workflows for downstream plan usage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| media file→decoder subprocess | Untrusted MP3 input is passed through ffmpeg/ffprobe |
| CLI args→filesystem | Book/chapter selectors resolve into local source/target paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | T | conversion output | mitigate | Enforce probe invariants (`pcm_s16le`, `16000`, mono) before accepting output file |
| T-03-06 | D | ffmpeg subprocess | mitigate | Bound subprocess runtime with timeout handling and fail-fast errors |
| T-03-07 | E | subprocess invocation | mitigate | Use argument-list subprocess calls (no shell interpolation) to avoid command injection |
| T-03-08 | R | metadata reporting | mitigate | Include source file path + measured values in CLI output for auditable runs |
</threat_model>

<verification>
- `uv run pytest tests/test_audio_convert.py tests/test_audio_metadata.py -x -v`
- `uv run bibliavox audio convert --book GEN --chapter 1`
- `uv run bibliavox audio info --book GEN --chapter 1`
- `go-task audio:convert`
</verification>

<success_criteria>
- Chapter MP3 is converted to WAV 16kHz mono PCM with strict post-checks
- Audio metadata command reports duration/bitrate/sample rate for chapter files
- Taskfile targets expose conversion/info flows for both CLI and pipeline usage
</success_criteria>

<output>
After completion, create `.planning/phases/03-audio-pipeline/03-02-SUMMARY.md`
</output>
