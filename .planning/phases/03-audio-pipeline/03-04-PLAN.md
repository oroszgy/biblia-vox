---
phase: 03-audio-pipeline
plan: 04
type: execute
wave: 4
depends_on: [03-03]
files_modified:
  - bibliavox/audio/downloader.py
  - bibliavox/cli/audio.py
  - Taskfile.yml
  - tests/test_audio_downloader.py
  - tests/test_audio_cli.py
  - .planning/ROADMAP.md
autonomous: true
gap_closure: true
requirements: [AUD-01, AUD-02, AUD-03]
must_haves:
  truths:
    - "User can run the documented Taskfile single-chapter download command and get retry/resume behavior"
    - "User can run the documented Taskfile batch download command and see visible progress indicators during execution"
    - "User can run the documented Taskfile single-chapter convert command and get verified WAV output"
  artifacts:
    - path: "Taskfile.yml"
      provides: "Executable task command contract for single/batch download and single convert"
      contains: "audio:download, audio:download-all, audio:convert"
    - path: "bibliavox/cli/audio.py"
      provides: "Rich progress-enabled batch download UX"
      exports: ["download"]
    - path: "bibliavox/audio/downloader.py"
      provides: "Batch download callback hooks to drive UI progress updates"
      exports: ["download_all"]
  key_links:
    - from: "bibliavox/cli/audio.py"
      to: "bibliavox/audio/downloader.py"
      via: "on_result callback updates progress per chapter"
      pattern: "on_result"
    - from: "Taskfile.yml"
      to: "bibliavox audio download"
      via: "task wrappers call CLI with documented arguments"
      pattern: "audio:download"
---

<objective>
Close Phase 3 verification gaps by fixing the task invocation contract and adding explicit progress indicators for batch audio download.

Purpose: make the roadmap acceptance path executable and observable so verify-work can confirm the same commands users are expected to run.

Output: updated Taskfile/ROADMAP command contract, progress-enabled batch download flow, and regression tests for the corrected behavior.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-audio-pipeline/03-CONTEXT.md
@.planning/phases/03-audio-pipeline/03-VERIFICATION.md
@.planning/phases/03-audio-pipeline/03-03-SUMMARY.md
@bibliavox/cli/audio.py
@bibliavox/audio/downloader.py
@Taskfile.yml

<interfaces>
From bibliavox/audio/downloader.py:
```python
def download_all(
    manifest: list[dict[str, Any]],
    output_root: Path,
    *,
    workers: int = 4,
    force: bool = False,
) -> BatchSummary
```

From Taskfile.yml (current execution style):
```yaml
audio:download:
  cmds:
    - uv run bibliavox audio download --book {{.BOOK}} --chapter {{.CHAPTER}}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: add batch progress instrumentation to download workflow</name>
  <files>bibliavox/audio/downloader.py, bibliavox/cli/audio.py, tests/test_audio_downloader.py, tests/test_audio_cli.py</files>
  <read_first>
    - .planning/phases/03-audio-pipeline/03-VERIFICATION.md (gap reason for missing progress indicator)
    - bibliavox/audio/downloader.py (current batch API)
    - bibliavox/cli/audio.py (current batch output path)
    - tests/test_audio_downloader.py and tests/test_audio_cli.py (existing regression style)
  </read_first>
  <behavior>
    - Test 1: `download_all` accepts a per-item callback and invokes it exactly once per manifest entry with final item status
    - Test 2: `audio download --all` shows active progress output while downloads run, not only final summary
    - Test 3: batch summary still reports downloaded/skipped/failed counts and exits non-zero when failures exist
  </behavior>
  <action>Extend `download_all` to accept an optional `on_result: Callable[[DownloadResult], None]` callback and invoke it after each chapter result is resolved. In `bibliavox/cli/audio.py`, wire Rich progress for `--all` mode (per D-11) so each callback advances visible progress and updates status text; preserve explicit mismatch diagnostics and failure summary output (per D-02). Keep idempotent default behavior with `--force` override unchanged (per D-12). Add/adjust tests first (RED→GREEN) to assert callback invocation and batch CLI progress output presence.</action>
  <verify>
    <automated>uv run pytest tests/test_audio_downloader.py tests/test_audio_cli.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "on_result" bibliavox/audio/downloader.py bibliavox/cli/audio.py` returns matches
    - `rg -n "Progress|SpinnerColumn|TaskProgressColumn" bibliavox/cli/audio.py` returns matches
    - `uv run pytest tests/test_audio_downloader.py tests/test_audio_cli.py -x -v` exits 0
  </acceptance_criteria>
  <done>Batch download path presents real progress indicators and remains deterministic for pass/fail accounting.</done>
</task>

<task type="auto">
  <name>task 2: standardize executable Taskfile command contract for verification commands</name>
  <files>Taskfile.yml, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/phases/03-audio-pipeline/03-VERIFICATION.md (failed command truths)
    - Taskfile.yml (current BOOK/CHAPTER/WORKERS variable contract)
    - .planning/ROADMAP.md (Phase 3 success criteria command text)
  </read_first>
  <action>Update Phase 3 success-criteria command text in `.planning/ROADMAP.md` to the executable go-task variable form and keep Taskfile targets split (per D-03, D-04). Use exact command forms: `task audio:download BOOK=GEN CHAPTER=1`, `task audio:download-all WORKERS=4`, and `task audio:convert BOOK=GEN CHAPTER=1`. In `Taskfile.yml`, add defaults so these commands run without undefined-variable failures (`WORKERS` default 4, and clear task descriptions that BOOK/CHAPTER are required for single-chapter tasks). Note in comments/descriptions that go-task flag syntax (`--book`) is not supported by task-level parser; using vars is intentional per tool behavior.</action>
  <verify>
    <automated>go-task --dry audio:download BOOK=GEN CHAPTER=1 && go-task --dry audio:download-all WORKERS=4 && go-task --dry audio:convert BOOK=GEN CHAPTER=1</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` Phase 3 success criteria contains `task audio:download BOOK=GEN CHAPTER=1`
    - `.planning/ROADMAP.md` Phase 3 success criteria contains `task audio:download-all WORKERS=4`
    - `.planning/ROADMAP.md` Phase 3 success criteria contains `task audio:convert BOOK=GEN CHAPTER=1`
    - `go-task --dry audio:download BOOK=GEN CHAPTER=1` exits 0
    - `go-task --dry audio:download-all WORKERS=4` exits 0
    - `go-task --dry audio:convert BOOK=GEN CHAPTER=1` exits 0
  </acceptance_criteria>
  <done>Roadmap truth commands and Taskfile invocation contract are aligned and executable in the actual task runner.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI batch mode→terminal output | Untrusted network download states are rendered in progress UI |
| task runner inputs→filesystem/network operations | User-supplied task variables trigger downloads/conversions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-13 | R | batch progress output | mitigate | Emit deterministic completion summary with downloaded/skipped/failed counts even when progress UI is enabled |
| T-03-14 | D | batch download execution | mitigate | Preserve worker bounds and non-zero exit on failures so failed batches are not treated as successful |
| T-03-15 | T | task contract drift | mitigate | Keep ROADMAP command strings synchronized to executable Taskfile commands and verify with `go-task --dry` checks |
</threat_model>

<verification>
- `uv run pytest tests/test_audio_downloader.py tests/test_audio_cli.py -x -v`
- `go-task --dry audio:download BOOK=GEN CHAPTER=1`
- `go-task --dry audio:download-all WORKERS=4`
- `go-task --dry audio:convert BOOK=GEN CHAPTER=1`
</verification>

<success_criteria>
- Batch download displays visible progress during execution and still reports deterministic final counts
- Phase 3 roadmap acceptance commands are executable with the documented Taskfile contract
- Verification gaps in `03-VERIFICATION.md` for command contract and progress indicators are closed
</success_criteria>

<output>
After completion, create `.planning/phases/03-audio-pipeline/03-04-SUMMARY.md`
</output>
