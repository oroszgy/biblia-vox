---
phase: 03-audio-pipeline
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified:
  - bibliavox/audio/seek_index.py
  - bibliavox/audio/pipeline.py
  - bibliavox/audio/__init__.py
  - bibliavox/cli/audio.py
  - Taskfile.yml
  - tests/test_audio_seek_index.py
  - tests/test_audio_pipeline.py
  - tests/test_audio_cli.py
autonomous: true
requirements: [AUD-05]
must_haves:
  truths:
    - "D-10: User can build sample-accurate seek index artifacts for prepared WAV chapters"
    - "D-07/D-08: User can run audio seek and get WAV-based preview output for a requested timestamp window"
    - "D-09/D-10/D-12: Prepared outputs include WAV + metadata + index sidecars under canonical paths with idempotent rerun semantics"
  artifacts:
    - path: "bibliavox/audio/seek_index.py"
      provides: "Sample-based index generation and seek-window extraction"
      exports: ["build_seek_index", "resolve_sample_window", "write_seek_preview"]
    - path: "bibliavox/audio/pipeline.py"
      provides: "Chapter prepare orchestration (convert + metadata + index)"
      exports: ["prepare_chapter"]
    - path: "bibliavox/cli/audio.py"
      provides: "audio prepare and audio seek commands"
      exports: ["prepare", "seek"]
  key_links:
    - from: "bibliavox/audio/pipeline.py"
      to: "bibliavox/audio/seek_index.py"
      via: "prepare flow writes index sidecar after conversion"
      pattern: "build_seek_index"
    - from: "bibliavox/cli/audio.py"
      to: "bibliavox/audio/seek_index.py"
      via: "seek command resolves sample window and writes preview wav"
      pattern: "resolve_sample_window|write_seek_preview"
---

<objective>
Implement WAV-based seek indexing and preview tooling for alignment-safe timestamp access.

Purpose: satisfy AUD-05 by producing deterministic seek index sidecars from prepared WAV files, exposing `audio prepare` and `audio seek` commands, and enforcing sample-based (not MP3 timebase) seek behavior.

Output: seek index module, prepare pipeline orchestration, seek/prepare CLI commands, and Taskfile targets for operational use.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-audio-pipeline/03-CONTEXT.md
@.planning/phases/03-audio-pipeline/03-RESEARCH.md
@.planning/phases/03-audio-pipeline/03-02-PLAN.md
@bibliavox/cli/audio.py

<interfaces>
From bibliavox/audio/convert.py (03-02):
```python
def convert_to_wav(input_mp3: Path, output_wav: Path, *, force: bool = False) -> Path
```

From bibliavox/audio/metadata.py (03-02):
```python
def probe_audio(path: Path) -> dict[str, Any]
```

Path contract (locked):
```text
data/raw/audio/{USX}/{chapter:03d}.mp3
data/prepared/audio/{USX}/{chapter:03d}.wav
data/prepared/audio/{USX}/{chapter:03d}.meta.json
data/prepared/audio/{USX}/{chapter:03d}.index.json
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: implement sample-accurate seek index and preview primitives</name>
  <files>bibliavox/audio/seek_index.py, tests/test_audio_seek_index.py</files>
  <read_first>
    - .planning/phases/03-audio-pipeline/03-CONTEXT.md (D-07, D-08, D-10 seek and sidecar contracts)
    - .planning/phases/03-audio-pipeline/03-RESEARCH.md (WAV sample indexing guidance)
    - bibliavox/audio/metadata.py (duration/sample-rate fields)
    - tests/test_audio_metadata.py (pytest style)
  </read_first>
  <behavior>
    - Test 1: build_seek_index writes index with sample_rate, total_samples, duration_sec, and path to wav file
    - Test 2: resolve_sample_window converts seconds→sample offsets deterministically and clamps bounds
    - Test 3: write_seek_preview extracts WAV window using sample offsets (no MP3-based seek math)
  </behavior>
  <action>Create `bibliavox/audio/seek_index.py` (per D-08, D-10) with `build_seek_index`, `resolve_sample_window`, and `write_seek_preview`. Index schema must include `sample_rate`, `total_samples`, `duration_sec`, `wav_path`, `book_usx`, `chapter`, and creation timestamp; write as `data/prepared/audio/{USX}/{chapter:03d}.index.json` (per D-09). Implement preview extraction using Python WAV readers against PCM sample offsets only; do not use MP3 timebase calculations. Add `tests/test_audio_seek_index.py` first (RED→GREEN).</action>
  <verify>
    <automated>uv run pytest tests/test_audio_seek_index.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "def build_seek_index\(" bibliavox/audio/seek_index.py` returns a match
    - `rg -n "def resolve_sample_window\(" bibliavox/audio/seek_index.py` returns a match
    - `rg -n "def write_seek_preview\(" bibliavox/audio/seek_index.py` returns a match
    - `rg -n "total_samples|sample_rate|duration_sec" bibliavox/audio/seek_index.py` returns matches
    - `uv run pytest tests/test_audio_seek_index.py -x -v` exits 0
  </acceptance_criteria>
  <done>Seek index and preview primitives are sample-accurate, deterministic, and independent of MP3 timestamp behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: implement prepare orchestration and CLI seek/prepare commands</name>
  <files>bibliavox/audio/pipeline.py, bibliavox/cli/audio.py, tests/test_audio_pipeline.py, tests/test_audio_cli.py, bibliavox/audio/__init__.py</files>
  <read_first>
    - bibliavox/audio/downloader.py (idempotent/force semantics)
    - bibliavox/audio/convert.py (conversion contract)
    - bibliavox/audio/metadata.py (metadata sidecar contract)
    - bibliavox/audio/seek_index.py (task 1 index and preview APIs)
    - bibliavox/cli/text.py (Typer + Rich command patterns)
  </read_first>
  <behavior>
    - Test 1: prepare_chapter runs convert→probe→index and writes wav/meta/index sidecars
    - Test 2: audio prepare defaults to skip-complete artifacts and supports --force reprocessing
    - Test 3: audio seek defaults to preview output window and reads index+WAV (not MP3)
  </behavior>
  <action>Create `bibliavox/audio/pipeline.py` with `prepare_chapter` that orchestrates conversion, metadata write (`.meta.json`), and index write (`.index.json`) for each chapter (per D-10). Extend `bibliavox/cli/audio.py` with `audio prepare` and `audio seek` (per D-04): `seek` must default to playback preview behavior by writing a requested window to an output WAV file (per D-07) using `resolve_sample_window` + `write_seek_preview` from task 1 (per D-08). Keep path layout fixed to raw/prepared contracts (per D-09). Make reruns idempotent by default with explicit `--force` override for prepare/seek artifacts (per D-12). Export new APIs via `bibliavox/audio/__init__.py`. Add/extend tests first (RED→GREEN).</action>
  <verify>
    <automated>uv run pytest tests/test_audio_pipeline.py tests/test_audio_cli.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "def prepare_chapter\(" bibliavox/audio/pipeline.py` returns a match
    - `rg -n "def prepare\(" bibliavox/cli/audio.py` returns a match
    - `rg -n "def seek\(" bibliavox/cli/audio.py` returns a match
    - `rg -n "index.json|meta.json" bibliavox/cli/audio.py bibliavox/audio/pipeline.py` returns matches
    - `uv run pytest tests/test_audio_pipeline.py tests/test_audio_cli.py -x -v` exits 0
  </acceptance_criteria>
  <done>Prepare and seek commands produce and consume WAV sidecars correctly, with preview-oriented default behavior.</done>
</task>

<task type="auto">
  <name>task 3: wire Taskfile seek/prepare targets and run end-to-end smoke checks</name>
  <files>Taskfile.yml</files>
  <read_first>
    - Taskfile.yml (existing task naming and deps patterns)
    - bibliavox/cli/audio.py (final command flags)
    - .planning/ROADMAP.md (Phase 3 success criteria commands)
  </read_first>
  <action>Add Taskfile targets `audio:prepare`, `audio:prepare-all`, and `audio:seek` aligned to the CLI contracts (per D-03, D-04). `audio:prepare` and `audio:prepare-all` must call Typer commands and keep idempotent default/`--force` override semantics (per D-12). `audio:seek` must include arguments for `--seconds`, `--duration-sec`, and `--output` to support preview verification flow (per D-07). Ensure commands operate on prepared WAV/index artifacts and never route seek through MP3 timebase logic (per D-08).</action>
  <verify>
    <automated>uv run bibliavox audio prepare --help && uv run bibliavox audio seek --help</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "audio:prepare:" Taskfile.yml` returns a match
    - `rg -n "audio:prepare-all:" Taskfile.yml` returns a match
    - `rg -n "audio:seek:" Taskfile.yml` returns a match
    - `rg -n "--duration-sec|--seconds|--output" Taskfile.yml` returns matches
    - `uv run bibliavox audio prepare --help` exits 0
    - `uv run bibliavox audio seek --help` exits 0
  </acceptance_criteria>
  <done>Taskfile exposes prepare and seek workflows required to verify AUD-05 behavior from CLI and task targets.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args→filesystem | User-provided book/chapter/time parameters drive local file reads/writes |
| prepared media→seek extraction | Untrusted/partial WAV artifacts are consumed to generate preview windows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09 | T | seek index sidecar | mitigate | Validate index schema fields and refuse seek when sample_rate/total_samples missing or invalid |
| T-03-10 | D | prepare-all orchestration | mitigate | Enforce bounded worker settings, idempotent skips, and explicit failure summary |
| T-03-11 | E | CLI path resolution | mitigate | Constrain output paths to project audio roots and reject traversal/absolute escapes |
| T-03-12 | R | seek preview behavior | mitigate | Emit deterministic seek report (source wav, offset samples, output path) for reproducible verification |
</threat_model>

<verification>
- `uv run pytest tests/test_audio_seek_index.py tests/test_audio_pipeline.py tests/test_audio_cli.py -x -v`
- `uv run bibliavox audio prepare --book GEN --chapter 1`
- `uv run bibliavox audio seek --book GEN --chapter 1 --seconds 120 --duration-sec 2 --output /tmp/gen1_120s.wav`
- `go-task audio:prepare --book GEN --chapter 1`
- `go-task audio:seek --book GEN --chapter 1 --seconds 120 --duration-sec 2 --output /tmp/gen1_120s.wav`
</verification>

<success_criteria>
- Chapter preparation produces WAV + metadata + index sidecars under `data/prepared/audio/{USX}/`
- Seek windows are computed and extracted from WAV sample offsets, not MP3 timestamps
- Default seek flow produces preview output artifact suitable for human spot checks
</success_criteria>

<output>
After completion, create `.planning/phases/03-audio-pipeline/03-03-SUMMARY.md`
</output>
